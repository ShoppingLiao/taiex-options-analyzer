# 日期顯示功能說明

## 📅 星期顯示功能

所有日期現在都會顯示中文星期，方便快速識別交易日。

## 顯示位置

### 1. 首頁 (index.html)
每個報告卡片的標題都會顯示日期 + 星期：
```
2026/01/09 (五)  ⭐ 最新報告
2026/01/08 (四)  歷史報告
2026/01/07 (三)  歷史報告
2026/01/06 (二)  歷史報告
2026/01/05 (一)  歷史報告
```

### 2. 報告頁面
Header 的 subtitle 會顯示：
```
交易日期: 2026/01/09 (五) | 契約月份: 202601 | 結算日: 2026/01/21 (二)
```

## 技術實現

### Python 輔助函數

在 `src/reporter.py` 中新增：

```python
def get_weekday_chinese(date_str: str) -> str:
    """將日期字串轉換為中文星期"""
    try:
        date_obj = datetime.strptime(date_str, '%Y%m%d')
        weekdays = ['一', '二', '三', '四', '五', '六', '日']
        return weekdays[date_obj.weekday()]
    except:
        return ""

def format_date_with_weekday(date_str: str) -> str:
    """格式化日期並加上星期"""
    try:
        date_obj = datetime.strptime(date_str, '%Y%m%d')
        weekday = get_weekday_chinese(date_str)
        return f"{date_obj.strftime('%Y/%m/%d')} ({weekday})"
    except:
        return date_str
```

### 模板變數

在 `_prepare_template_data()` 中新增：
```python
'date_with_weekday': format_date_with_weekday(result.date),
```

### 首頁生成腳本

新增 `generate_index_with_weekday.py` 腳本，可自動生成帶星期的首頁。

使用方式：
```bash
python3 generate_index_with_weekday.py
```

## 星期計算邏輯

使用 Python 的 `datetime.weekday()` 方法：
- 0 = 星期一
- 1 = 星期二
- 2 = 星期三
- 3 = 星期四
- 4 = 星期五
- 5 = 星期六
- 6 = 星期日

對應到中文：
```python
weekdays = ['一', '二', '三', '四', '五', '六', '日']
```

## 實際範例

### 2026/01 週的交易日
- 2026/01/05 (一) - 開始一週
- 2026/01/06 (二)
- 2026/01/07 (三)
- 2026/01/08 (四)
- 2026/01/09 (五) - 最新報告
- 2026/01/21 (二) - 結算日

## 自動化流程

1. **生成報告時**：
   ```bash
   python3 generate_batch_reports.py 20260105 20260106 ...
   ```
   - 自動計算並加入星期
   - 在報告 header 顯示

2. **更新首頁時**：
   ```bash
   python3 generate_index_with_weekday.py
   ```
   - 讀取報告列表
   - 為每個日期計算星期
   - 生成新的 index.html

## 視覺效果

### 首頁卡片
```
┌─────────────────────────┐
│ 2026/01/09 (五)        │ ← 藍色加粗
│ 202601 月份契約         │
│ [最新報告] 🔥          │
└─────────────────────────┘
```

### 報告頁面 Header
```
══════════════════════════════════════════
        台指選擇權分析報告
    交易日期: 2026/01/09 (五)
   契約月份: 202601 | 結算日: 2026/01/21 (二)
══════════════════════════════════════════
```

## 優點

1. ✅ **快速識別**：一眼看出是週幾
2. ✅ **交易規劃**：方便規劃週一開盤、週五結算等策略
3. ✅ **使用者體驗**：更符合台灣市場習慣
4. ✅ **自動計算**：無需手動輸入，減少錯誤
5. ✅ **一致性**：首頁和報告頁面格式統一

## 未來擴展

可以考慮加入：
- 🗓️ 農曆日期
- 📊 當週已交易天數
- ⏰ 距離結算日天數
- 🎯 週漲跌幅統計
