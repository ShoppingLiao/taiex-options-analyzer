# /ai-train - AI 學習與訓練

執行 AI 預測、檢討與學習流程，持續改進預測能力。

## 使用方式

```
/ai-train                    # 執行完整訓練流程（所有可用數據）
/ai-train predict 20260113   # 僅生成指定日期的預測
/ai-train review 20260112    # 僅執行指定日期的檢討
/ai-train settlement 20260115 20260113,20260114  # 生成結算日預測
/ai-train settlement-review 20260115 data/pdf/xxx.pdf  # 結算日檢討
```

## 功能模式

### 1. 完整訓練流程 (`/ai-train`)

執行所有可用數據的預測→檢討→學習循環：

```bash
python3 process_all_predictions.py
```

流程：
1. 讀取所有可用的 PDF 數據
2. 按時間順序生成預測
3. 用後一日實際數據檢討前一日預測
4. 自動更新學習系統
5. 顯示學習統計

### 2. 單日預測 (`/ai-train predict YYYYMMDD`)

為指定日期生成明日預測：

1. 確認 PDF 存在：`data/pdf/期貨選擇權盤後日報_{日期}.pdf`
2. 執行預測生成（透過 process_all_predictions.py 或直接調用 AIPredictionGenerator）
3. 預測保存至：`data/ai_learning/predictions/prediction_{日期}.json`

### 3. 單日檢討 (`/ai-train review YYYYMMDD`)

檢討指定日期的預測準確度：

1. 讀取預測：`data/ai_learning/predictions/prediction_{日期}.json`
2. 讀取實際數據（需要後一日 PDF）
3. 執行檢討分析
4. 保存結果：`data/ai_learning/reviews/review_{日期}.json`
5. 更新學習系統

### 4. 結算日預測 (`/ai-train settlement 結算日 分析日期`)

生成結算日預測：

```bash
python3 generate_settlement_predictions.py
```

參數：
- `結算日`: YYYYMMDD 格式
- `分析日期`: 逗號分隔的兩個日期

保存至：`data/ai_learning/settlement_predictions/settlement_prediction_{日期}.json`

### 5. 結算日檢討 (`/ai-train settlement-review 結算日 PDF路徑`)

檢討結算日預測：

```bash
python3 generate_settlement_review.py \
    --settlement-date {結算日} \
    --pdf-path {PDF路徑}
```

## AI 學習系統說明

### 經驗等級
- 🌱 新手（<5 筆記錄）
- 📚 學習中（5-14 筆）
- 🎯 進階（15-29 筆）
- ⭐ 專家（30-49 筆）
- 👑 大師（≥50 筆）

### 學習維度
- PC Ratio 模式識別
- 市場情緒與趨勢組合
- 成功預測特徵提取
- 歷史相似案例匹配

### 數據存儲位置
```
data/ai_learning/
├── analysis_records.json       # 分析記錄
├── learned_insights.json       # 學習洞察
├── predictions/                # 每日預測
├── reviews/                    # 每日檢討
├── settlement_predictions/     # 結算預測
└── settlement_reviews/         # 結算檢討
```

## 注意事項

- 訓練需要足夠的歷史數據（至少 2 天以上）
- 檢討需要有對應的預測記錄和實際數據
- 學習系統會自動從記錄中提取洞察（需 ≥5 筆記錄）
